class PlotBuilder:
    @staticmethod
    def create_language_histogram(df: pd.DataFrame, language: str) -> plt.Figure:
        """Гистограмма распределения баллов по языку"""
        pass

    @staticmethod
    def create_ranking_heatmap(df: pd.DataFrame) -> plt.Figure:
        """Тепловая карта позиций участников"""
        pass
